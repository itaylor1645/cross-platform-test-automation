# Allows @Tag filtering via -Dgroups isn't a thing in JUnit5; use Maven include/exclude tags below or CLI selectors.
# This file is still useful for consistent configuration later.
junit.jupiter.execution.parallel.enabled=false
